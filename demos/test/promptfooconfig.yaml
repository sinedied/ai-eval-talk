# Learn more about building a configuration: https://promptfoo.dev/docs/configuration/guide
description: "My eval"

prompts:
  - file://prompt.md

providers:
  - ollama:chat:phi3
  # - ollama:chat:phi3:medium
  - ollama:chat:llama2
  # - ollama:chat:llama3
  # - ollama:chat:llama3.1
  # - ollama:chat:mistral
  # - ollama:chat:mistral:v0.2
  - ollama:chat:gemma2
  - id: azureopenai:chat:gpt-4o-mini
    config:
      apiHost: 'cog-zolnwmeatemto.openai.azure.com'
  - id: azureopenai:chat:gpt-35-turbo
    config:
      apiHost: 'cog-zolnwmeatemto.openai.azure.com'

tests:
  - vars:
      question: What shoes should I use for running?
    assert:
      # For more information on assertions, see https://promptfoo.dev/docs/configuration/expected-outputs

      # Make sure output contains the word "avocado"
      - type: icontains
        value: avocado

      # Prefer shorter outputs
      - type: javascript
        value: 1 / (output.length + 1)